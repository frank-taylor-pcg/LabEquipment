namespace Enums
{	
	// Not liking the name, but this is the list of states all of the processes can be in
	public enum AxisId
	{
		None
		, TrayElevator
		, TBar
		, Offloading
		, FrontPickhead
		, RearPickhead
	}
}
